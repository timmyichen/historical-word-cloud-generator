<!DOCTYPE html>
<html lang="en">
<head>
  {% include "partials/head.njk" %}
  <title>{{appname}} - API</title>
</head>
<body style="padding:5%;">
  <h1 class="ui header">{{appname}}</h1>
  <h2 class="ui header">API Entry Point</h2>
  <p style="padding-bottom: 30px">
    Welcome to the API for this application.
    This is a JSON API with routes listed below.
  </p>
  <table class="ui celled table">
    <thead>
      <tr>
        <th>Title</th>
        <th>Route Name</th>
        <th>Methods</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      {% for route in routes %}
      <tr>
        <td>{{route.title}}</td>
        <td>{{route.routename}}</td>
        <td>{{ route.methods.join(", ") }}</td>
        <td>
          {% for line in route.description %}
            {{ line }}<br>
          {% endfor %}
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</body>
</html>